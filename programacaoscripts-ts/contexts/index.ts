import {AuthContext, AuthProvider, AuthProviderType} from './Auth';
import { PetContext, PetProvider } from './PetContext';

export type AuthType = AuthProviderType

export {
  AuthContext,
  AuthProvider,
  PetContext,
  PetProvider
}